<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xoi.smvitm">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Edit_New_Faculty_Profile"></activity>
        <activity android:name=".Faculty_profile_activity" />
        <activity android:name=".Popup_timetable_activity" />
        <activity android:name=".Event_popup_activity" />
        <activity android:name=".login_Activity" />
        <activity android:name=".User_profile_Activity" />
        <activity android:name=".Edit_Existing_User_Profile_Activity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".splash_Activiy">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>